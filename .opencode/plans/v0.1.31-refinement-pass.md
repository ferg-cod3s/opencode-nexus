# v0.1.31 Release Plan — Refinement Pass

**Date:** 2026-04-26
**Target:** `0.1.31-dev31` (marketing `0.1.31`, build `31`)
**Phases:** 0 → 1 → 2 → 3 → 4 → 5 (locked order)
**Canonical plan:** `.opencode/plans/v0.1.31-release-plan.md`
**Source:** Original Ultraplan output preserved as `.opencode/plans/1777219716707-swift-rocket.md` (deleted after consolidation)

---

## How To Resume This Document With Any Model

If you're picking this up partway through execution, the contract is:

1. **Read the canonical plan first:** `.opencode/plans/v0.1.31-release-plan.md` — locked decisions, high-level phases.
2. **This file is the EXECUTION SPEC.** Each phase has atomic tasks with file:line citations, acceptance criteria, dependencies, and confidence ratings. Use the "Specific Gaps Resolution" table near the end to confirm hidden assumptions.
3. **Cross-Phase Corrections (next section) ARE BLOCKING.** The canonical plan has 5 verified factual errors. Read C-1 through C-5 before touching any code — they will silently break your build if ignored.
4. **Spot-checks performed (2026-04-26):** All 5 corrections (C-1 through C-5) were verified by reading the actual source files. The `connection_manager.rs:624` reqwest call, `chat.astro:211` phantom `logout_user` invoke, `runner-detection.yml:124` macos-14 fallback, and `build-ios-archive.sh:18` orphan entitlements var are all real.
5. **Recommended execution order:** See "Execution Strategy" at the end of this file (4 sessions, Phase 2 isolated).

---

## Cross-Phase Corrections (MUST read before execution)

The canonical plan contains **5 factual errors** that will break the build if followed literally:

| ID | Canonical Plan Says | Reality | Evidence | Resolution |
|---|---|---|---|---|
| C-1 | Remove `reqwest` from Cargo.toml | `connection_manager.rs:624` uses `reqwest::get()` for health checks + `error.rs:229` has `From<reqwest::Error>` | `src-tauri/src/connection_manager.rs:624`, `src-tauri/src/error.rs:229` | KEEP `reqwest` |
| C-2 | Remove `sentry` from Cargo.toml | `main.rs:28-63` initializes Sentry with live DSN | `src-tauri/src/main.rs:28` | KEEP `sentry` + `sentry-anyhow` |
| C-3 | (not mentioned) | `build-ios-archive.sh:18` sets `CODE_SIGN_ENTITLEMENTS` but NEVER passes it to xcodebuild (lines 87-96) | `scripts/build-ios-archive.sh:18,87-96` | Add entitlements flag to xcodebuild |
| C-4 | (not mentioned) | `chat.astro:211` calls `invoke('logout_user')` — phantom command, NOT in retained 11 | `frontend/src/pages/chat.astro:211` | Replace with `disconnect_from_server` |
| C-5 | (not mentioned) | `runner-detection.yml:124` falls back to `macos-14` — violates self-hosted-only policy | `.github/workflows/runner-detection.yml:124` | Delete file or remove fallback |

### Dependency Post-Deletion Map

After deleting 7 Rust modules, these Cargo.toml deps status:

| Dependency | Used By Surviving Code? | Action |
|---|---|---|
| `reqwest` (json, rustls-tls) | `connection_manager.rs:24,624`, `error.rs:229` | **KEEP** |
| `sentry` / `sentry-anyhow` | `main.rs:28-63,66-71` | **KEEP** |
| `tokio` | `connection_manager.rs` tests use `#[tokio::test]`, async commands | **KEEP** |
| `uuid` | AUDIT — check if connection_manager uses it | Conditional |
| `argon2` | No surviving file (auth.rs previously removed) | REMOVE |
| `rand` | No surviving file (auth.rs previously removed) | REMOVE |
| `reqwest-eventsource` | Only `streaming_client.rs` (DELETED) | REMOVE |
| `futures-util` / `futures` | Only streaming/event modules (DELETED) | REMOVE |
| `urlencoding` | Only `api_client.rs` (DELETED) | REMOVE |
| `sysinfo` | Only `model_manager.rs` (DELETED) | REMOVE |

---

## Phase 0: Security & Cleanup

### Pre-flight

- [ ] Apple Developer portal access verified (can log in to developer.apple.com)
- [ ] 1Password vault or GitHub Secrets ready for new credentials
- [ ] No open PRs against main that would conflict
- [ ] Self-hosted runner confirmed online (GitHub Settings → Actions → Runners)
- [ ] `cargo check && cargo test` baseline green (Phase 0 is read-only validation first)

### Atomic Tasks

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 0.1.1 | Run full baseline: cargo check + test | none | — | Exit 0, record output | 10m | 0.95 |
| 0.1.2 | Run frontend baseline: bun install + typecheck + test + lint | none | — | Exit 0, record output | 10m | 0.95 |
| 0.1.3 | Rotate Apple Developer API key (revoke `AuthKey_J94Q923ZNG.p8`) | none | Apple Developer portal | New key generated, stored in 1Password/GitHub Secrets | 15m | 0.90 |
| 0.1.4 | Revoke provisioning profile `Opencode_Nexus_App_Store_V2.mobileprovision` | none | Apple Developer portal | Profile revoked, new one created if needed | 10m | 0.90 |
| 0.1.5 | Verify `test_cert.pem` / `test_key.pem` are test-only; revoke if real | none | — | Confirmed test-only or revoked | 5m | 0.95 |
| 0.1.6 | Store new credentials outside repo (1Password + GitHub Secrets) | 0.1.3, 0.1.4 | — | Secrets stored in `SENTRY_AUTH_TOKEN`, `APPLE_API_*`, `IOS_CERTIFICATE` etc. | 10m | 0.90 |
| 0.1.7 | Add `*.pem` and `.env.*` to `.gitignore` | none | `.gitignore` | Patterns appended after line 177 | 2m | 0.99 |
| 0.1.8 | Purge secret files from git history via `git filter-repo` | 0.1.3-0.1.6 | git history | `git log --all -- AuthKey_J94Q923ZNG.p8` returns empty | 15m | 0.85 |
| 0.1.9 | Force-push rewritten history, notify collaborators to re-clone | 0.1.8 | remote | Push succeeds, collaborators notified | 5m | 0.90 |
| 0.1.10 | Archive 66 root markdown files → `docs/archive/sessions/` | none | Root dir | `ls *.md \| wc -l` < 10. Keep: README, CLAUDE, AGENTS, LICENSE, CHANGELOG, RELEASE-PLAN, CONTRIBUTING, SECURITY (if exists) | 15m | 0.95 |
| 0.1.11 | Consolidate 5 root `build-ios*.sh` scripts (delete them) | none | Root dir | Only `scripts/build-ios-archive.sh` and `scripts/upload-testflight.sh` survive | 5m | 0.90 |
| 0.1.12 | Delete dead JS: `frontend/src/chat.js`, `frontend/src/login.js` | none | `frontend/src/` | Files deleted, no import references to them | 3m | 0.95 |
| 0.1.13 | Update `scripts/README.md` setup instructions to reference env vars / 1Password vault, not literal repo paths | 0.1.6 | `scripts/README.md:23,33,84,85` | No mention of `AuthKey_J94Q923ZNG.p8` or `Opencode_Nexus_App_Store_V2.mobileprovision`; instead points to `$APPLE_API_KEY_PATH` / 1Password entry | 10m | 0.90 |
| 0.1.14 | Fix `scripts/verify-signing-assets.sh` to read from env var | 0.1.6 | `scripts/verify-signing-assets.sh:25` | `PROFILE_PATH="${IOS_MOBILE_PROVISION:-$HOME/.config/opencode-nexus/profile.mobileprovision}"`; script exits clean if file missing with helpful message | 10m | 0.90 |

### Rollback

1. **git filter-repo:** Cannot undo — this rewrites history. Ensure secrets are rotated BEFORE running. Force-push to restore previous ref if needed (but secrets would still be in old history).
2. **File moves/deletes:** `git checkout HEAD -- .` restores all deleted/moved files
3. **If mid-execution:** Stop. `git stash` any in-progress changes. Restore from last commit.

### Hidden Dependencies Discovered

- `.gitignore:168-177` — Already has `*.p8`, `*.p12`, `*.mobileprovision`, plus patterns for `*_COMPLETE.md`, `*_REPORT.md` etc. Many of the 66 root .md files were committed before these rules were added.
- `ios-release.yml:24` — `fetch-depth: 0` checks out full history. After filter-repo, next CI run gets clean history (fine).
- No commit SHA pins in any workflow (except `apache/skywalking-eyes@v0.6.0` in license-check.yml). filter-repo won't break action refs.
- `test-backend.yml` uses `hashFiles('**/Cargo.lock')` for caching. After history rewrite, caches just rebuild (no breakage).
- **`scripts/README.md:23,33,84,85` references the soon-deleted secret filenames** — setup instructions tell users to `export APP_STORE_CONNECT_API_KEY_PATH=".../AuthKey_J94Q923ZNG.p8"`. After deletion this doc is stale. **Add task: update setup docs to reference 1Password vault path or env var lookup, not literal repo paths.** (Discovered post-Ultraplan during local verification, 2026-04-26.)
- **`scripts/verify-signing-assets.sh:25` hardcodes `Opencode_Nexus_App_Store_V2.mobileprovision`** as `PROFILE_PATH`. After Phase 0 deletion, this script reports a missing file every time it runs. **Add task: update script to read from `$IOS_MOBILE_PROVISION` env var or new path under `~/.config/opencode-nexus/`.** (Discovered post-Ultraplan, 2026-04-26.)

### Phase Confidence: 0.90

**Top uncertainties:**
1. Whether all 4 secret files still exist in history (vs already cleaned)
2. Whether `git filter-repo` is installed (`pip install git-filter-repo`)
3. Collaborator impact — anyone with clones must re-clone

---

## Phase 1: Version & iOS Config Unification

### Pre-flight

- [ ] Phase 0 complete, git history clean
- [ ] Build 30 confirmed in App Store Connect (delivery UUID `5df48246-4464-437f-89a0-75a8b8877afe`)
- [ ] Apple Distribution certificate valid in keychain (`security find-identity -v -p codesigning`)

### Atomic Tasks

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 1.1.1 | Set root `package.json` version to `0.1.31-dev31` | PF-1 | `package.json:4` | `grep '"version"' package.json` shows `0.1.31-dev31` | 1m | 0.99 |
| 1.1.2 | Set `frontend/package.json` version to `0.1.31` | PF-1 | `frontend/package.json:4` | `grep '"version"' frontend/package.json` shows `0.1.31` | 1m | 0.99 |
| 1.1.3 | Set `src-tauri/Cargo.toml` version to `0.1.31` | PF-1 | `src-tauri/Cargo.toml:3` | `version = "0.1.31"` | 1m | 0.99 |
| 1.1.4 | Set `src-tauri/tauri.conf.json` version to `0.1.31` | PF-1 | `src-tauri/tauri.conf.json:4` | `"version": "0.1.31"` | 1m | 0.99 |
| 1.1.5 | Fix entitlements passthrough in `build-ios-archive.sh` | PF-1 | `scripts/build-ios-archive.sh:87-96` | `CODE_SIGN_ENTITLEMENTS` flag passed to xcodebuild archive command | 5m | 0.85 |
| 1.1.6 | Sync gen/apple entitlements from ios-config | PF-1 | `src-tauri/gen/apple/src-tauri_iOS/src-tauri_iOS.entitlements` | File matches `src-tauri/ios-config/src-tauri_iOS.entitlements` (network.client=true, keychain, team-id) | 2m | 0.95 |
| 1.1.7 | Fix gen/apple `ExportOptions.plist` method | PF-1 | `src-tauri/gen/apple/ExportOptions.plist` | `<string>debugging</string>` → `<string>app-store</string>` | 1m | 0.99 |
| 1.1.8 | Update gen/apple `project.yml` MARKETING_VERSION | PF-1 | `src-tauri/gen/apple/project.yml:16` | `MARKETING_VERSION: 0.1.31` | 1m | 0.99 |
| 1.1.9 | Verify build script version extraction | 1.1.1 | — | Dry-run `./scripts/build-ios-archive.sh` shows `IOS_MARKETING_VERSION=0.1.31` and `IOS_BUILD_NUMBER=31` | 5m | 0.95 |
| 1.1.10 | Verify `bun run tauri ios dev` builds simulator | 1.1.1-1.1.8 | — | Simulator launches without version warnings | 10m | 0.80 |

### Rollback

```bash
git checkout -- package.json frontend/package.json src-tauri/ scripts/
```

### Hidden Dependencies Discovered

- `build-ios-archive.sh:51-57` — Regex `^(\d+\.\d+\.\d+)-dev(\d+)$` is the ONLY pattern that produces correct build numbers. Version WITHOUT `-dev` suffix falls to else branch → `build = 1` → App Store Connect REJECTS (must be > 30). `0.1.31-dev31` matches correctly.
- `build-ios-archive.sh:18` — Sets `CODE_SIGN_ENTITLEMENTS` but never passes it to xcodebuild (lines 87-96). **Entitlements are NOT applied.** This is a pre-existing bug that must be fixed here.
- `build-ios-archive.sh:14` — References `src-tauri/ios-config/ExportOptions.plist` (correct, `app-store-connect` method), NOT the gen/apple one (`debugging` method). The gen/apple fix (1.1.7) is for consistency only; actual builds use ios-config version.
- `frontend/package.json:4` — Currently `0.1.4`. No workspace deps reference the version field. Safe to change.
- `src-tauri/gen/apple/project.yml` — Overridden at build time by xcodebuild args. Setting it is for consistency, not functional.

### Phase Confidence: 0.88

**Top uncertainties:**
1. Whether entitlements passthrough fix actually applies entitlements to the binary (needs local archive test)
2. Whether `bun run tauri ios dev` works after version changes without regenerating Xcode project
3. Whether `bun.lockb` needs updating after package.json version change

---

## Phase 2: Rust Backend Simplification

### Pre-flight

- [ ] Phase 1 complete, `cargo check` passes
- [ ] Git working tree clean (commit Phase 1 first)
- [ ] Frontend baseline green from Phase 0

### Atomic Tasks

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 2.1.1 | Audit all frontend invoke() calls — confirm only 11 retained | none | — | `grep -rh "invoke(" frontend/src --include='*.ts' --include='*.svelte' --include='*.astro' \| grep -oE "invoke\(['\"]\w+" \| sort -u` lists only retained commands + known dead ones (dashboard/test-chat/settings pages to be deleted) | 10m | 0.99 |
| 2.1.2 | Quarantine legacy pages: delete dashboard.astro, test-chat.astro, settings.astro | 2.1.1 | `frontend/src/pages/{dashboard,test-chat,settings}.astro` | Files deleted; `logs.astro` kept (invokes only retained commands) | 2m | 0.99 |
| 2.1.3 | Fix `chat.astro:211` — replace `logout_user` with `disconnect_from_server` | 2.1.1 | `frontend/src/pages/chat.astro:211,224` | No phantom invoke() calls remain; redirect goes to `/connect` not `/login` | 10m | 0.90 |
| 2.1.4 | Delete 7 dead Rust modules | 2.1.1 | `src-tauri/src/{api_client,chat_client,chat_client_example,event_bridge,model_manager,session_manager,streaming_client}.rs` | `ls src-tauri/src/*.rs` shows only: `lib.rs`, `connection_manager.rs`, `error.rs`, `main.rs` | 5m | 0.99 |
| 2.1.5 | Clean `lib.rs` — remove dead mod declarations (lines 23-30) | 2.1.4 | `src-tauri/src/lib.rs:23-30` | Keep only `mod connection_manager;` and `mod error;` | 3m | 0.99 |
| 2.1.6 | Clean `lib.rs` — remove dead use imports (lines 32-40) | 2.1.4 | `src-tauri/src/lib.rs:32-40` | Keep only `connection_manager::*` imports; remove all references to deleted modules | 5m | 0.95 |
| 2.1.7 | Clean `lib.rs` — remove dead state structs (lines 52-60) | 2.1.4 | `src-tauri/src/lib.rs:52-60` | Keep only `ConnectionManagerState` | 3m | 0.99 |
| 2.1.8 | Remove `greet` command from lib.rs | 2.1.5 | `src-tauri/src/lib.rs:133-136` | Function deleted | 2m | 0.99 |
| 2.1.9 | Remove 16 dead Tauri commands from lib.rs | 2.1.5-2.1.7 | `src-tauri/src/lib.rs:427-855` | All commands referencing deleted modules removed. Keep only: `connect_to_server`, `test_server_connection`, `get_connection_status`, `get_current_connection`, `disconnect_from_server`, `get_saved_connections`, `save_connection`, `get_last_used_connection`, `get_application_logs`, `log_frontend_error`, `clear_application_logs` | 15m | 0.95 |
| 2.1.10 | Update `run()` setup closure in lib.rs | 2.1.7, 2.1.9 | `src-tauri/src/lib.rs:857-1008` | Remove all state init for dead modules (ApiClient, EventBridge, SessionManager, ModelManager, StreamingClient, ChatClient). Keep only ConnectionManager init. Remove dead `.manage()` calls. | 15m | 0.88 |
| 2.1.11 | Update `generate_handler!` macro | 2.1.8, 2.1.9 | `src-tauri/src/lib.rs:972-1005` | Only 11 retained command names in handler list. `greet` removed. | 5m | 0.99 |
| 2.1.12 | Fix inline tests in lib.rs | 2.1.9 | `src-tauri/src/lib.rs:1010-1300` | Delete: `test_greet_command` (1040-1043), `test_model_config_serialization` (1168-1185), `test_model_config_optional` (1188-1205). Update tests referencing deleted types. Keep tests for retained functions. | 15m | 0.82 |
| 2.1.13 | Trim `connection_manager.rs` — remove chat/streaming helpers if present | 2.1.4 | `src-tauri/src/connection_manager.rs` | File contains only persistence + status code paths. No session/streaming/model helpers. | 15m | 0.85 |
| 2.1.14 | Update Cargo.toml deps | 2.1.4 | `src-tauri/Cargo.toml` | Remove: `argon2`, `rand`, `reqwest-eventsource`, `futures-util`, `futures`, `urlencoding`, `sysinfo`. KEEP: `reqwest`, `sentry`, `sentry-anyhow`, `tokio`, `uuid` (if used). | 10m | 0.90 |
| 2.1.15 | Verify `cargo check` passes | 2.1.5-2.1.14 | — | Exit 0 | 5m | 0.85 |
| 2.1.16 | Verify `cargo test` passes | 2.1.15 | — | Exit 0, all tests pass | 5m | 0.80 |
| 2.1.17 | Verify `cargo clippy` — zero errors | 2.1.15 | — | Exit 0 | 5m | 0.85 |
| 2.1.18 | Verify frontend build + tests | 2.1.2-2.1.3 | — | `cd frontend && bun run build && bun test && bun run typecheck` all pass | 10m | 0.85 |
| 2.1.19 | Grep verification — only retained invoke() calls | 2.1.2, 2.1.3 | — | `grep -rh "invoke(" frontend/src --include='*.ts' --include='*.svelte' --include='*.astro' \| grep -oE "invoke\(['\"]\w+" \| sort -u` → only 11 retained names | 5m | 0.95 |
| 2.1.20 | Verify total LOC drop | 2.1.4 | — | `wc -l src-tauri/src/*.rs` total ≈ 1,500 (from 6,646) | 2m | 0.99 |
| 2.1.21 | Update `status_docs/TODO.md` to reflect actual state | 2.1.20 | `status_docs/TODO.md` | No false claims about deleted modules; reflects reality | 10m | 0.90 |

### Rollback

```bash
# Nuclear option — restores all Rust files and Cargo.toml
git checkout -- src-tauri/ frontend/src/pages/ frontend/src/chat.js frontend/src/login.js
cargo build  # verify rollback worked
```

**Note:** This phase has the highest risk. Commit after each logical group (deletions, lib.rs cleanup, Cargo.toml, frontend) so you can cherry-pick rollback points.

### Hidden Dependencies Discovered

- `src-tauri/src/error.rs:229-231` — `impl From<reqwest::Error> for AppError`. error.rs is RETAINED. reqwest MUST stay.
- `src-tauri/src/main.rs:28-63` — Sentry init with RAII guard. sentry crate MUST stay. This is main.rs, not lib.rs, but they share Cargo.toml.
- `src-tauri/src/main.rs:74` — `src_tauri_lib::setup_panic_hook()` — calls into lib.rs. Verify this function survives cleanup.
- `src-tauri/src/lib.rs:42` — `use chrono::Utc` used by retained `log_to_file` function. Keep chrono dep.
- `src-tauri/src/lib.rs:46` — `use std::sync::Arc` used by retained ConnectionManagerState. Keep.
- `connection_manager.rs` has 12 inline tests (lines 671-973) — these survive with the file. No extraction needed.
- `chat_client_example.rs` is NOT declared as a module in lib.rs (orphan). Safe to delete directly.
- `chat.astro:211` — `invoke('logout_user')` is a PHANTOM command — never existed in lib.rs. Would throw runtime error. Must replace.
- `chat.astro:214` — `sentry.setUser(null)` — Sentry integration. If Astro Sentry is disabled, these calls may fail silently or throw.
- `chat.astro:49-532` — Extensive Sentry usage via `getSentry()` helper. Must audit for runtime errors if Sentry is misconfigured.

### Phase Confidence: 0.85

**Top uncertainties:**
1. Whether `connection_manager.rs` has chat/streaming helpers that need trimming (file is 973 lines — seems large for just persistence)
2. Whether inline tests in lib.rs reference more deleted types than identified
3. Whether removing `uuid` from Cargo.toml breaks connection_manager (needs grep)

---

## Phase 3: iOS Build Verification

### Pre-flight

- [ ] Phase 2 complete, `cargo check && cargo test && cargo clippy` all pass
- [ ] `rustup target add aarch64-apple-ios aarch64-apple-ios-sim` (if not already)
- [ ] Apple Distribution certificate valid in keychain
- [ ] Provisioning profile installed (from Phase 0 rotation)

### Atomic Tasks

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 3.1.1 | Simulator build: `bun run tauri ios dev` | PF-3 | — | Boots without signing errors, app renders | 10m | 0.80 |
| 3.1.2 | Release build: `./scripts/build-ios-archive.sh` | PF-3 | — | `.xcarchive` + IPA produced, exit 0 | 20m | 0.75 |
| 3.1.3 | Validate IPA — version strings | 3.1.2 | — | `unzip -p *.ipa Payload/*.app/Info.plist \| plutil -p -` shows `CFBundleShortVersionString=0.1.31`, `CFBundleVersion=31` | 5m | 0.90 |
| 3.1.4 | Validate IPA — entitlements | 3.1.2 | — | `codesign -d --entitlements - Payload/*.app` shows network.client=true, keychain access | 5m | 0.85 |
| 3.1.5 | Validate IPA — dSYMs present | 3.1.2 | — | `ls build/*.xcarchive/dSYMs/` shows .dSYM bundles | 2m | 0.95 |
| 3.1.6 | Validate IPA — bundle ID | 3.1.2 | — | `CFBundleIdentifier = com.agentic-codeflow.opencode-nexus` | 2m | 0.99 |
| 3.1.7 | Verify CI uses self-hosted runner | PF-3 | — | `grep 'runs-on' .github/workflows/ios-release.yml` shows `[self-hosted, macOS, ARM64]` | 2m | 0.99 |

### Rollback

No code changes in this phase — only verification. If build fails, debug and fix in the relevant previous phase.

### Hidden Dependencies Discovered

- `scripts/build-ios-archive.sh:14` — References `src-tauri/ios-config/ExportOptions.plist` which has `uploadSymbols: true`. dSYMs are generated as part of the archive process.
- `src-tauri/gen/apple/project.yml` — Contains preBuildScript running `cargo tauri ios xcode-script`. CI patches this with sed (`ios-release.yml:77`). Local builds use it as-is.
- No TestFlight upload in this phase — Phase 5 owns that.

### Phase Confidence: 0.78

**Top uncertainties:**
1. Whether local iOS archive succeeds after entitlements fix (first attempt is the real test)
2. Whether provisioning profile from Phase 0 rotation is correctly installed
3. Xcode version compatibility with Tauri 2 generated project

---

## Phase 4: Mobile UI Polish & Final Validation

### Pre-flight

- [ ] Phase 3 complete, valid IPA produced
- [ ] Physical iOS device available for testing
- [ ] Source icon image ready for `bun run tauri icon`

### Atomic Tasks

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 4.1.1 | Audit touch targets ≥ 44px in all Svelte components | none | `frontend/src/lib/components/` + `frontend/src/components/` | All interactive elements have `min-height: 44px; min-width: 44px` | 30m | 0.85 |
| 4.1.2 | Add iOS safe area CSS — `env(safe-area-inset-*)` padding | none | Layout components | Content not obscured by notch/home indicator on iPhone with notch | 20m | 0.80 |
| 4.1.3 | Generate splash screen + icon set via `bun run tauri icon` | none | `src-tauri/icons/` | Icons committed, appear correctly on home screen | 15m | 0.90 |
| 4.1.4 | Verify no PWA components (ServiceWorker, PWAInstallPrompt) | none | — | `grep -r "ServiceWorkerRegistration\|PWAInstallPrompt" frontend/` returns nothing | 2m | 0.99 |
| 4.1.5 | Physical device test — connect to opencode serve | 4.1.1-4.1.3 | — | Connection establishes, status shows connected | 10m | 0.75 |
| 4.1.6 | Physical device test — chat with streaming | 4.1.5 | — | Messages send/receive, SSE streaming visible | 10m | 0.75 |
| 4.1.7 | Physical device test — session switch + model switch | 4.1.5 | — | Both work without errors | 10m | 0.75 |
| 4.1.8 | Physical device test — background/foreground reconnect | 4.1.5 | — | App reconnects after background → foreground | 5m | 0.70 |
| 4.1.9 | Full quality gate | 4.1.1-4.1.4 | — | `cargo clippy && cargo test && cd frontend && bun run lint && bun run typecheck && bun test` — all pass with zero failures | 10m | 0.85 |

### Rollback

```bash
git checkout -- frontend/src/ src-tauri/icons/
```

### Hidden Dependencies Discovered

- PWA components already absent (0 matches for ServiceWorkerRegistration/PWAInstallPrompt). No work needed.
- `offline-storage.ts` handles offline state via localStorage with gzip compression. No migration needed between builds.
- Safe area handling requires viewport meta tag with `viewport-fit=cover` — verify this exists in Astro layout.

### Phase Confidence: 0.80

**Top uncertainties:**
1. Whether touch target audit finds violations in existing components
2. Whether background/foreground reconnect works reliably on iOS
3. Whether icon source image is available/appropriate

---

## Phase 5: TestFlight Beta Program Operations

### Pre-flight

- [ ] Phases 0-4 complete
- [ ] Local IPA validated (Phase 3 checks pass)
- [ ] Physical device test passed (Phase 4)
- [ ] App Store Connect API key valid and not expired
- [ ] Sentry org/project access confirmed

### P5.1 — dSYM Upload to Sentry

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.1.1 | Create Sentry auth token (scope: `project:releases`) | PF-5 | Sentry UI | Token stored as GitHub Secret `SENTRY_AUTH_TOKEN` | 5m | 0.95 |
| 5.1.2 | Add `SENTRY_ORG`, `SENTRY_PROJECT` to GitHub Secrets | PF-5 | GitHub Settings | Secrets set | 3m | 0.99 |
| 5.1.3 | Write `scripts/upload-dsyms-sentry.sh` | none | `scripts/upload-dsyms-sentry.sh` | Script wraps `sentry-cli upload-dif` over `.xcarchive/dSYMs/` | 10m | 0.95 |
| 5.1.4 | Install sentry-cli on self-hosted runner | none | Runner machine | `brew install getsentry/tools/sentry-cli` succeeds, `sentry-cli --version` exits 0 | 5m | 0.90 |
| 5.1.5 | Insert dSYM upload step in `ios-release.yml` between archive and TestFlight upload | 5.1.3 | `.github/workflows/ios-release.yml` | New step after archive, before upload | 5m | 0.90 |

### P5.2 — Version Bump Helper

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.2.1 | Write `scripts/bump-ios-version.sh` | none | `scripts/bump-ios-version.sh` | `--build` increments dev suffix; `--marketing X.Y.Z` updates marketing version; writes to package.json + tauri.conf.json + Cargo.toml atomically; refuses dirty tree; `--dry-run` prints changes | 30m | 0.85 |
| 5.2.2 | Verify bump helper dry-run | 5.2.1 | — | `./scripts/bump-ios-version.sh --build --dry-run` prints `0.1.31-dev31 → 0.1.31-dev32` | 5m | 0.90 |

### P5.3 — Release Runbook & Changelog

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.3.1 | Update `CHANGELOG.md` with v0.1.31 section | none | `CHANGELOG.md` | `## [0.1.31] - 2026-04-26` section added with bullets for: dead module cleanup, connection-only architecture, CI hardening, entitlements fix, version unification. Stale `[Unreleased]` entries cleaned. | 15m | 0.95 |
| 5.3.2 | Write `docs/RELEASE_TESTFLIGHT.md` (≤1 page runbook) | none | `docs/RELEASE_TESTFLIGHT.md` | Steps: bump version → update changelog → commit → tag → push → watch CI → verify App Store Connect → run checklist → promote | 15m | 0.95 |

### P5.4 — In-App Feedback

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.4.1 | Create `frontend/src/lib/build-info.ts` | none | `frontend/src/lib/build-info.ts` | Surfaces marketing version + build number from Vite env vars | 15m | 0.90 |
| 5.4.2 | Create `frontend/src/components/FeedbackButton.svelte` | 5.4.1 | `frontend/src/components/FeedbackButton.svelte` | Opens `mailto:beta@<domain>` prefilled with build/OS/device via `@tauri-apps/plugin-opener` | 20m | 0.85 |
| 5.4.3 | Add FeedbackButton to settings or chat header | 5.4.2 | Settings/chat page | Button visible and functional | 10m | 0.85 |

### P5.5 — Tester Docs & Templates

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.5.1 | Write `docs/BETA_PROGRAM.md` | none | `docs/BETA_PROGRAM.md` | Invite flow, tester expectations, triage process (Sentry for crashes, mailto for UX) | 20m | 0.95 |
| 5.5.2 | Write `docs/RELEASE_NOTES_TEMPLATE.md` | none | `docs/RELEASE_NOTES_TEMPLATE.md` | Sections: New / Known issues / What to test | 10m | 0.95 |

### P5.6 — Smoke Test Checklist

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.6.1 | Write `docs/SMOKE_TEST_IOS.md` | none | `docs/SMOKE_TEST_IOS.md` | ≤10 min checklist: cold launch <2s, connect, chat/streaming, background/foreground, force-quit/reopen, feedback button | 15m | 0.95 |

### P5.7 — Build Acceptance Checklist

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.7.1 | Write `docs/TESTFLIGHT_BUILD_CHECKLIST.md` | none | `docs/TESTFLIGHT_BUILD_CHECKLIST.md` | Post-build, pre-promotion: correct versions, dSYMs in Sentry, install on device, run smoke test, paste release notes | 10m | 0.95 |

### P5.8 — Final Upload & Verification

| ID | Title | Depends | Files Touched | Acceptance Criteria | Est | Conf |
|---|---|---|---|---|---|---|
| 5.8.1 | Run full quality gate | 5.1-5.7 | — | `cargo clippy && cargo test && cd frontend && bun run lint && bun run typecheck && bun test` — all pass | 10m | 0.85 |
| 5.8.2 | Build final IPA | 5.8.1 | — | `./scripts/build-ios-archive.sh` succeeds, version 0.1.31 build 31 | 20m | 0.75 |
| 5.8.3 | Upload to TestFlight | 5.8.2 | — | `./scripts/upload-testflight.sh` succeeds | 15m | 0.70 |
| 5.8.4 | Verify App Store Connect processing | 5.8.3 | — | Build 31 appears, processing completes | 15m | 0.75 |
| 5.8.5 | Run `SMOKE_TEST_IOS.md` on TestFlight build | 5.8.4 | — | All checklist items pass on real device via TestFlight | 15m | 0.70 |
| 5.8.6 | Tag release | 5.8.5 | Git | `git tag ios-v0.1.31 -m "Release 0.1.31"` | 2m | 0.99 |

### Rollback

1. **Before TestFlight upload:** Revert Phase 5 changes, re-verify locally
2. **After TestFlight upload:** Expire build in App Store Connect, push fix as build 32
3. **After tag:** `git tag -d ios-v0.1.31 && git push origin :refs/tags/ios-v0.1.31`

### Hidden Dependencies Discovered

- `CHANGELOG.md` EXISTS (181 lines) but is stale (last entry `[0.0.2] - 2025-09-05`). Gap from 0.0.2 to 0.1.31 — document as "development builds, no formal release between versions."
- Sentry uses 3 different project DSNs: backend project 16 (`main.rs`), frontend manual project 14 (`sentry.init.ts`), frontend Astro project 17 (`sentry.client.config.ts`). Recommend consolidating or documenting.
- `upload-testflight.sh:67` — References app ID `6754924026` in App Store Connect URL. Verify correct.
- `upload-testflight.sh:51` — Uses `xcrun altool` (deprecated but still functional for iOS). Apple recommends altool for iOS uploads until notarytool gains iOS support.
- `sentry-cli` NOT installed on self-hosted runner. Must install via brew in P5.1.4.
- `@sentry/astro` integration is COMMENTED OUT in `astro.config.mjs:76-82`. For dSYM symbolication (Rust crashes), only the backend Sentry (project 16) matters. Frontend Sentry is separate.
- `docs/RELEASE_VERSIONING.md` is in the canonical plan's "new files" list but not critical for TestFlight. Can be deferred.

### Phase Confidence: 0.78

**Top uncertainties:**
1. Whether TestFlight upload succeeds on first try (altool can be flaky with network)
2. Whether sentry-cli dSYM upload works correctly with self-hosted Sentry instance
3. Whether App Store Connect accepts build 31 without export compliance issues

---

## Specific Gaps Resolution

| Gap | Finding | Resolution |
|---|---|---|
| Build-30→31 migration | No IndexedDB; localStorage schema is simple (VERSION='1.0.0' no-op migration); `server_connections.json` format unchanged | **No migration needed.** Safe upgrade. |
| Test coverage for 11 retained commands | `connection_manager.rs` has 12 inline tests (lines 671-973) — survive with file. `lib.rs` tests reference `ModelConfig` from deleted `api_client.rs` — MUST delete/update those tests. | Tasks 2.1.12 handles lib.rs test cleanup. |
| `frontend/package.json` workspace settings | No monorepo workspace config. No deps reference version field. | Safe to align to `0.1.31`. Task 1.1.2. |
| `tauri-plugin-store` conflict | NOT installed. Canonical plan keeps `connection_manager.rs`. RELEASE-PLAN.md mentions plugin but is superseded. | **Stick with connection_manager.rs.** No plugin adoption. |
| `sentry-cli` on runner | NOT installed. No CI step installs it. | Task 5.1.4: `brew install getsentry/tools/sentry-cli`. Task 5.1.5: add CI step. |
| `git filter-repo` + CI | No SHA pins (except one in license-check.yml). Cargo.lock caches just rebuild. fetch-depth:0 gets clean history. | **Safe.** No CI breakage from history rewrite. |
| `greet` command removal | Defined at `lib.rs:133-136`, registered at line 973, tested at lines 1040-1043. NOT invoked by any frontend code. | Task 2.1.8 removes it. Safe. |
| `build-ios-archive.sh` regex robustness | Falls to `build=1` if no `-dev` suffix → App Store Connect REJECT. `0.1.31-dev31` matches correctly. | **Version MUST include `-devNNN`.** Plan uses correct format. |
| `CHANGELOG.md` existence | EXISTS, 181 lines, stale (last: 0.0.2 from Sept 2025). | Task 5.3.1 updates it. No creation needed. |

---

## Structural Concerns

1. **connection_manager.rs is 973 lines** for persistence + status. Seems oversized. Consider future refactor to reduce, but not blocking for v0.1.31.
2. **3 Sentry project DSNs** across frontend/backend. Should consolidate to single project for v0.2. Not blocking for v0.1.31.
3. **`runner-detection.yml` macos-14 fallback** is a policy violation. Deleting the file (task 4.1) is the simplest fix.
4. **`upload-testflight.sh` uses deprecated `altool`**. Works today but Apple may deprecate further. Monitor for v0.2.

---

## Final Self-Evaluation

- **Overall plan executability:** 0.85
- **Most likely failure point:** Phase 3 task 3.1.2 (local iOS archive build) — entitlements fix is untested; first attempt may need iteration.
- **Recommended kickoff phase:** Phase 0 (security & cleanup first)
- **Estimated total effort:** ~9 focused hours (vs canonical plan's 12-20 days — this refinement is more granular and realistic for a single engineer with tooling support)

### Execution Strategy

1. **Session 1:** Phases 0-1 (security + version config). Commit after each phase.
2. **Session 2:** Phase 2 (Rust cleanup — highest risk, own session). Commit in logical groups.
3. **Session 3:** Phase 3 (iOS build verification). Iterate on entitlements fix if needed.
4. **Session 4:** Phase 4 (CI hardening) + Phase 5 (TestFlight beta ops).
5. **Merge to main** only after TestFlight processing succeeds.
