# Sentry Configuration for OpenCode Nexus
# ==========================================
#
# Frontend DSN: https://1ca61080ceb639661e4da7e91914cb92@sentry.fergify.work/17
# Backend DSN: https://f7ded2177f996e519be91651f05d38a3@sentry.fergify.work/16
#
# Auth Token for Source Map Uploads (required for production builds)
# ==========================================
# To get your Sentry auth token:
# 1. Go to https://sentry.fergify.work/settings/account/api/auth-tokens/
# 2. Create a new Auth Token with these scopes:
#    - project:releases (read & write)
#    - org:read
# 3. Copy the token and paste it below
# 4. For development, this is optional (errors still captured)
# 5. For production builds, this is required for source map uploads
#
# Replace with your actual Sentry auth token from https://sentry.fergify.work/settings/account/api/auth-tokens/
# Token format: sntrys_...
#
SENTRY_AUTH_TOKEN=

# Optional: Override environment (defaults to auto-detected from build)
# SENTRY_ENVIRONMENT=development

# Optional: Override trace sample rate (defaults to sentry.client.config.ts)
# SENTRY_TRACE_SAMPLE_RATE=1.0
