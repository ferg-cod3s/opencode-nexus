# Act CLI Configuration for OpenCode Nexus
# This file configures Docker images for local GitHub Actions testing

# Use the official act images for Ubuntu and macOS runners
-P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest
-P ubuntu-22.04=ghcr.io/catthehacker/ubuntu:full-latest
-P macos-14=ghcr.io/catthehacker/ubuntu:full-latest
-P macos-13=ghcr.io/catthehacker/ubuntu:full-latest

# Reuse containers between runs for faster testing
reuse-containers

# Use ARM64 architecture (for Apple Silicon Macs)
container-architecture linux/arm64

# Enable verbose output for debugging
verbose

# Bind current directory for file access
bind

# Use specific secrets file for local testing (uncomment when needed)
# secret-file .secrets.local